import { Component } from '@angular/core';

@Component({
  // to use file name and not use long path
  moduleId: module.id,
  selector: 'my-app',
  templateUrl: `app.component.html`,
})
export class AppComponent  {  }
